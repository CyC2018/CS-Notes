^(.*)$
^foo
bar$

----------------------------------------------------

[
	["regex", "^(.*)$"],
	["regex", "^foo"],
	["regex", "bar$"]
]

----------------------------------------------------

Checks for regex.